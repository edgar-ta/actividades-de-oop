El profe no explicó nada interesante hoy xd

```ts
const message: string = "I just think we'd get along, you know?";
let count: number = 0;

class Something {
	constructor(firstValue: number) {
		this.firstValue = firstValue;
	}
	getFirstValue(): number {
		return this.firstValue;
	}
}

const something: Something = new Something(1);
console.log(`The first value is: ${something.getFirstValue()}`);

console.log("What the fuck is going on today?");
```
